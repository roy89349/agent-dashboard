# ── secrets & per-install config (NEVER commit) ──
config.local.env
mission-control/.env*.local
.env
.env.*
!.env.example

# ── runtime state (generated by the fleet) ──
/control/
/state/
/logs/
/worktrees/
/data/

# ── design artifact with absolute paths (regenerate locally) ──
CONTROL-PLANE-SPEC.json

# ── node / Next.js (mission-control) ──
node_modules/
mission-control/.next/
mission-control/next-env.d.ts
mission-control/*.tsbuildinfo
**/*.log

# ── OS ──
.DS_Store
Thumbs.db
